import javax.swing.*;
import javax.swing.ImageIcon.*;
import java.awt.*;
public class CreateFrame {
    public static void main(String[] args) {

       /* JFrame frame = new JFrame();  //creates
        frame.setTitle("Jframe window");  //title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setResizable(false);  //prevents resizing of frame
        frame.setSize(420, 420);  //dimension of frame
        frame.setVisible(true);    //makes frame visible

        ImageIcon image=new ImageIcon("javaimg.png");
        frame.setIconImage(image.getImage());
        frame.getContentPane().setBackground(Color.BLUE);*/
        new CreateFrame_2();
    }
}
